# Examples

Small, shareable sample models and command lines will live here.

Large or private city models must not be committed to the repository. Keep them
outside the repo and pass their path on the command line:

```bash
citysmith /path/to/your_city_lod3.gml -o your_city_lod2.gml --report report.json
```
